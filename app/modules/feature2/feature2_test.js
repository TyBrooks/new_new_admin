'use strict';

describe('adminApp.feature2 module', function() {

  beforeEach(module('adminApp.feature2'));

  describe('feature2 controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view2Ctrl = $controller('Feature2Ctrl');
      expect(view2Ctrl).toBeDefined();
    }));

  });
});